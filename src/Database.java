package Maze

public class Database {
     ArandomTable = new Table (1,1);
        string name

    class Table extends Database
    {
        int rows;
        int cols;

        static void Table (rows,cols)
        {
            this.rows = rows;
        }

    }

    static void Database (name)
    {
        name=name
    }

    static bool SendToDatabase(somedata)....

}
